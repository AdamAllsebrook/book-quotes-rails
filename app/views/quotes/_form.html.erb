<%= form_with(model: quote, data: { turbo_frame: 'quotes' }, class: "space-y-5 flex flex-col border-y-2 p-4") do |form| %>
  <% if quote.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
        <% quote.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex flex-col">
    <%= form.label :book %>
    <%= form.text_field :book %>
  </div>

  <div class="flex flex-col">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>
  
  <%= form.submit %>
<% end %>
